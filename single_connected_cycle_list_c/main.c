// Single connected cyclic list (last node's pointer has a root node's address)
#include <stdio.h>
#include <stdlib.h>
#include <cassert>

typedef struct node_t {
	int number;
	struct node_t* next_node_ptr;
} node_t;

node_t* init_list(int number) {

}

void add_node(node_t* list, int number) {
	if (list != NULL) {
		
	}
}



int main() {

	return 0;
}
